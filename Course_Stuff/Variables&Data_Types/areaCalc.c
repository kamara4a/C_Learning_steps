#include <stdio.h>  //imports standard I/O libraries

int main(){
    int length = 34;
    int width = 23;

    int area = length * width;

    printf("the area is: %dcm^2 \n", area);

    return 0; //return 0 to tell OS there's no problem
}